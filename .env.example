# ════════════════════════════════════════════════════════════════
# 🔧 CLASSHUB API - Configuration Environment Variables
# ════════════════════════════════════════════════════════════════
#
# 📝 Instructions:
#   1. Copier ce fichier: cp .env.example .env
#   2. Remplir les valeurs selon votre environnement
#   3. Ne JAMAIS commiter le fichier .env (déjà dans .gitignore)
#
# ════════════════════════════════════════════════════════════════

# ──────────────────────────────────────────────────────────────
# 🌍 APPLICATION
# ──────────────────────────────────────────────────────────────

# Environment: development, staging, production
NODE_ENV=development

# Port d'écoute du serveur (défaut: 3000)
PORT=3000

# ──────────────────────────────────────────────────────────────
# 🗄️  DATABASE (PostgreSQL)
# ──────────────────────────────────────────────────────────────

# Host de la base de données
# - Local: localhost
# - Docker: postgres (nom du service dans docker-compose.yml)
DB_HOST=localhost

# Port PostgreSQL (défaut: 5432)
DB_PORT=5432

# Nom d'utilisateur PostgreSQL
DB_USERNAME=classhub_user

# Mot de passe PostgreSQL
# ⚠️  IMPORTANT: Utiliser un mot de passe fort en production!
DB_PASSWORD=classhub_password_dev

# Nom de la base de données
DB_DATABASE=classhub_dev

# ──────────────────────────────────────────────────────────────
# 🔐 JWT AUTHENTICATION
# ──────────────────────────────────────────────────────────────

# Secret pour signer les tokens JWT
# ⚠️  CRITIQUE: Générer un secret aléatoire fort (minimum 32 caractères)
# Exemple de génération: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production_min_32_chars

# Durée de validité de l'access token
# Exemples: 15m (15 minutes), 1h (1 heure), 7d (7 jours)
# ℹ️  Recommandé: 15m pour sécurité optimale avec refresh token
JWT_EXPIRES_IN=15m

# ──────────────────────────────────────────────────────────────
# 🔄 REFRESH TOKEN (Optionnel - Gestion dans le code)
# ──────────────────────────────────────────────────────────────

# Durée de validité du refresh token (en jours)
# Valeur par défaut dans le code: 30 jours
# REFRESH_TOKEN_EXPIRY_DAYS=30

# ──────────────────────────────────────────────────────────────
# 🌐 CORS (Cross-Origin Resource Sharing)
# ──────────────────────────────────────────────────────────────

# Origines autorisées (séparées par des virgules)
# Exemples:
#   - Local: http://localhost:4200,http://localhost:3001
#   - Production: https://app.classhub.com,https://www.classhub.com
# CORS_ORIGINS=http://localhost:4200,http://localhost:3001

# ──────────────────────────────────────────────────────────────
# 📧 EMAIL (À implémenter - Phase 2)
# ──────────────────────────────────────────────────────────────

# SMTP Host (ex: smtp.gmail.com, smtp.sendgrid.net)
# SMTP_HOST=smtp.example.com

# SMTP Port (587 pour TLS, 465 pour SSL)
# SMTP_PORT=587

# SMTP User
# SMTP_USER=noreply@classhub.com

# SMTP Password
# SMTP_PASSWORD=your_smtp_password

# Email expéditeur par défaut
# EMAIL_FROM=ClassHub <noreply@classhub.com>

# ──────────────────────────────────────────────────────────────
# 📊 MONITORING & LOGGING (À implémenter - Phase 2)
# ──────────────────────────────────────────────────────────────

# Sentry DSN pour tracking des erreurs
# SENTRY_DSN=https://xxxxx@sentry.io/xxxxx

# Niveau de log: error, warn, info, debug, verbose
# LOG_LEVEL=info

# ──────────────────────────────────────────────────────────────
# 🔧 REDIS (Cache - À implémenter - Phase 2)
# ──────────────────────────────────────────────────────────────

# Redis Host
# REDIS_HOST=localhost

# Redis Port
# REDIS_PORT=6379

# Redis Password (optionnel)
# REDIS_PASSWORD=

# ──────────────────────────────────────────────────────────────
# 📦 STORAGE (Upload fichiers - À implémenter - Phase 2)
# ──────────────────────────────────────────────────────────────

# Type de storage: local, s3, gcs
# STORAGE_TYPE=local

# Pour S3/AWS
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_S3_BUCKET=
# AWS_S3_REGION=eu-west-1

# ──────────────────────────────────────────────────────────────
# 🚀 AUTRES CONFIGURATIONS
# ──────────────────────────────────────────────────────────────

# Rate limiting (requests per minute)
# RATE_LIMIT_TTL=60
# RATE_LIMIT_MAX=100

# Taille maximale des requêtes (en bytes)
# MAX_REQUEST_SIZE=10485760

# ════════════════════════════════════════════════════════════════
# 📚 EXEMPLES PAR ENVIRONNEMENT
# ════════════════════════════════════════════════════════════════

# ──────────────────────────────────────────────────────────────
# DÉVELOPPEMENT LOCAL (avec Docker)
# ──────────────────────────────────────────────────────────────
# NODE_ENV=development
# PORT=3000
# DB_HOST=localhost
# DB_PORT=5432
# DB_USERNAME=classhub_user
# DB_PASSWORD=classhub_password_dev
# DB_DATABASE=classhub_dev
# JWT_SECRET=dev_secret_key_change_in_production
# JWT_EXPIRES_IN=15m

# ──────────────────────────────────────────────────────────────
# PRODUCTION
# ──────────────────────────────────────────────────────────────
# NODE_ENV=production
# PORT=3000
# DB_HOST=your-rds-endpoint.region.rds.amazonaws.com
# DB_PORT=5432
# DB_USERNAME=classhub_prod
# DB_PASSWORD=STRONG_PASSWORD_HERE
# DB_DATABASE=classhub_prod
# JWT_SECRET=GENERATE_RANDOM_SECRET_64_CHARS_MINIMUM
# JWT_EXPIRES_IN=15m
# CORS_ORIGINS=https://app.classhub.com
# SENTRY_DSN=https://xxxxx@sentry.io/xxxxx

# ════════════════════════════════════════════════════════════════
# 🔐 SÉCURITÉ - BONNES PRATIQUES
# ════════════════════════════════════════════════════════════════
#
# 1. JWT_SECRET:
#    - Minimum 32 caractères (recommandé: 64+)
#    - Générer avec crypto.randomBytes()
#    - Différent entre dev/staging/prod
#    - Ne JAMAIS partager ou commiter
#
# 2. DB_PASSWORD:
#    - Minimum 16 caractères
#    - Mélange majuscules/minuscules/chiffres/symboles
#    - Utiliser un gestionnaire de mots de passe
#
# 3. Variables sensibles en production:
#    - Utiliser des secrets managers (AWS Secrets Manager, etc.)
#    - Variables d'environnement du système (Kubernetes secrets)
#    - Ne pas stocker en clair dans le code
#
# 4. Rotation des secrets:
#    - JWT_SECRET: Rotation tous les 6-12 mois
#    - DB_PASSWORD: Rotation annuelle minimum
#
# ════════════════════════════════════════════════════════════════
# 📖 DOCUMENTATION
# ════════════════════════════════════════════════════════════════
#
# - README.md: Guide complet de setup
# - docs/REFRESH_TOKEN.md: Système d'authentification
# - docs/MULTI_TENANT.md: Architecture multi-tenant
# - docs/SWAGGER.md: Documentation API
#
# 🌐 Swagger UI: http://localhost:3000/api
#
# ════════════════════════════════════════════════════════════════
